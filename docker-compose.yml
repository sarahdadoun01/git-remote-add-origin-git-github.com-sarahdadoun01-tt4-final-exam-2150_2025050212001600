version: '3.8'

services:
  backend:
    build: ./backend/FitnessTrackerAPI
    ports:
      - "5000:8080"

  frontend:
    build: ./frontend/fitness-tracker-frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
